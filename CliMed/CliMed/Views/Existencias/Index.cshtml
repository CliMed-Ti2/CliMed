@model IEnumerable<CliMed.Models.Existencias>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClinicaFK)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProdutoFK)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClinicaFK)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProdutoFK)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdExistencia">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdExistencia">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdExistencia">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
